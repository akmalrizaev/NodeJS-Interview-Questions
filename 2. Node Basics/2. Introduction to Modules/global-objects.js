// Global Objects

// "global" object is global scope so we don't need
// to import using the require() statement

// Web browser -> "window" object
// Node -> "global" object

console.log(global);

console.log(module);

// the "__filename" gives the current path & file name.
console.log(__filename);
console.log(__dirname);
